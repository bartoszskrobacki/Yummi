---
export interface Props {
	icon: string
}

const { icon, ...attributes } = Astro.props as Props
const { default: innerHTML } = await import(`./../../media/icons/${icon}.svg?raw`)
---

<Fragment set:html={innerHTML} />
